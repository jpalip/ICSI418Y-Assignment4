<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Assignment 4</title>
    <meta charset="utf-8" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Assignment4</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="#">Home</a>
            </li>
          </ul>
          </form>
        </div>
      </div>
    </nav>
    <div class="row">
        <% wizards.forEach(function(wizard){ %>
            <li>
              
              <%= wizard.name %>
    
              
              <% if (wizard.house == "Gryffindor") { %>
               <span style="color:red"> <%= " house: " + wizard.house %></span>
              
              <% } else { %>
                <span style="color:green"> <%= " house: " + wizard.house %></span>
               <% } %> 
            
            
            </li>
    
          <% }); %>
      <div class="card col-sm">{user.imageURL}</div>
      <div class="card col-sm">{user.fullName}</div>
      <div class="card col-sm">{user.fullBio}</div>
    </div>
  </body>
</html>
